__pycache__/
*.pyc
*.pyo
instance/
*.db-journal
.env

# Patient database — must never be committed. It is live clinical data,
# and tracking it makes "git reset --hard" overwrite the real database.
clinic.db
backups/

# Weekly Google Drive backup: per-install marker + the single-file staging
# folder the receptionist drags into Drive. Neither should ever be committed.
.monday_backup_popup_marker
weekly_backup_upload/
